import { LightningElement, api } from 'lwc';
export default class lwc3LDSEditRecord extends LightningElement {
    @api recordId;

    handleSubmit() {
        // Code that runs when form is submitted.
    }
}